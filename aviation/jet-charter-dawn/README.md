<div align="center">

# ✈️ ALTIUS AIR

**3D-лендинг для частной чартерной авиации**

Сайт работает как короткий фильм: камера пролетает сквозь облака к джету на рассвете,
а дальше вы «снижаетесь» по секциям с помощью обычного скролла — до формы заявки на рейс.

<img src="../../docs/previews/jet-charter-dawn.jpg" width="720" alt="ALTIUS AIR — первый экран: джет на рассвете над облаками">

</div>

> [!NOTE]
> Это **шаблон**, а не сайт реальной компании. Название «ALTIUS AIR», самолёты, отзывы,
> цифры и контакты — вымышленные заглушки. Ниже расписано по шагам, как заменить их на своё.

---

## 🚀 Как запустить

Понадобится любой локальный сервер — двойной клик по `index.html` **не сработает**
(браузеры блокируют 3D-модули у файлов, открытых «с диска», адрес `file://`).

**Вариант 1 — терминал (самый простой):**

```bash
cd aviation/jet-charter-dawn
npx serve .
```

Откройте адрес, который появится в терминале (обычно `http://localhost:3000`).

**Вариант 2 — VS Code / WebStorm:**
правый клик по `index.html` → «Open with Live Server» (в WebStorm — открыть через встроенный браузерный сервер).

**Вариант 3 — Python (если есть):**

```bash
cd aviation/jet-charter-dawn
python3 -m http.server 8000
# открыть http://localhost:8000
```

> [!IMPORTANT]
> Нужен интернет: Three.js, GSAP, Lenis и шрифты подключаются через CDN.

---

## ✏️ Как сделать сайт своим

Всё редактируется в обычном текстовом редакторе. Основной файл — `index.html`,
цвета и шрифты — в начале `css/style.css`. Идите по шагам:

### Шаг 1. Название компании

Откройте `index.html` и через поиск (`Ctrl+F` / `Cmd+F`) найдите все `ALTIUS`.
Название встречается в четырёх местах:

| Где | Что искать |
|---|---|
| Вкладка браузера | `<title>` в самом верху файла |
| Экран загрузки | блок `id="preloader"` |
| Шапка сайта | ссылка `class="brand"` в `<header>` |
| Подвал | ещё одна ссылка `class="brand"` внизу + строка копирайта |

### Шаг 2. Тексты и карточки

Каждая секция в `index.html` подписана своим `id` — просто найдите нужную и поменяйте текст внутри:

| Секция | `id` в файле | Что внутри |
|---|---|---|
| Первый экран | `id="hero"` | Заголовок, подзаголовок, кнопки |
| Направления | `id="destinations"` | Карточки городов/маршрутов |
| Флот | `id="fleet"` | Карточки самолётов |
| Сервис и отзывы | `id="service"` | Услуги и цитаты клиентов |
| Безопасность | `id="safety"` | Цифры и сертификаты |
| Заявка | `id="request"` | Форма и контакты |

Все места с выдуманным контентом помечены прямо в тексте словами **«ДЕМО»** и **«ЗАГЛУШКА»** —
пройдитесь поиском по этим словам, чтобы ничего не пропустить.

### Шаг 3. Контакты

Телефон, почта и мессенджеры находятся в двух местах: блок `class="request__contacts"`
(рядом с формой) и подвал `<footer>`.

### Шаг 4. Форма заявки

Сейчас форма — демонстрационная: при отправке показывает «успех», но **данные никуда не уходят**
(обработчик — в `js/main.js`, блок с комментарием «демо-форма»). Чтобы заявки реально приходили,
подключите любой сервис форм ([Formspree](https://formspree.io/), [Web3Forms](https://web3forms.com/) и т.п.)
или свой бэкенд: добавьте форме `action` или замените обработчик `submit` на отправку `fetch`-ом.

### Шаг 5. Цвета и шрифты (по желанию)

Вся палитра собрана в самом начале `css/style.css`, в блоке `:root` — поменяли значение
в одном месте, и оно поменялось по всему сайту:

```css
:root {
  --c-night: #0B1E3B;  /* тёмно-синий фон */
  --c-steel: #3E6FA3;  /* стально-голубой (небо, полутона) */
  --c-white: #F5F8FC;  /* цвет текста */
  --c-gold:  #C9A24B;  /* золотой акцент: кнопки, детали */
}
```

Шрифты — там же, строками ниже (`--font-display`, `--font-body`, `--font-mono`).
Сейчас это Manrope, Golos Text и IBM Plex Mono — все с поддержкой кириллицы,
подключаются в `<head>` файла `index.html`.

> [!TIP]
> Цвета 3D-сцены (небо, солнце, облака) задаются отдельно — в `js/scene.js`.
> Если меняете палитру сайта кардинально, загляните и туда.

### Шаг 6. Своя 3D-модель самолёта или своё небо (по желанию)

Сейчас вся графика рисуется кодом — внешних файлов нет вообще. Если хотите вставить
настоящую 3D-модель (GLB) или панораму неба (HDRI) — см. короткий гайд в [`assets/README.md`](assets/README.md).

---

## 📁 Что в какой папке

```
index.html      вся разметка страницы + подключение библиотек с CDN
css/style.css   все стили: палитра, адаптив, фолбэки
js/main.js      «дирижёр»: прелоадер, запуск сцены и скролла, форма, очистка
js/scene.js     3D-сцена: небо, солнце, облака, джет, постобработка
js/scroll.js    скролл-анимации: маршрут камеры, появление контента, HUD высоты
assets/         пусто — вся графика процедурная; сюда кладите свои модели/текстуры
```

---

## 🔧 Технические детали

<details>
<summary><b>3D-сцена и постобработка</b></summary>
<br>

- Процедурное рассветное небо (шейдерный купол) + PMREM-окружение из него же —
  HDRI-подобное освещение без единого внешнего файла.
- Море облаков из спрайтов с процедурной canvas-текстурой, экспоненциальный туман.
- Стилизованный джет из примитивов: золотые детали, проблесковые огни, инверсионные следы.
- Постобработка: UnrealBloom (мягкое солнце), BokehPass (глубина резкости),
  кастомная цветокоррекция под палитру, виньетка, лёгкое зерно.
</details>

<details>
<summary><b>Кинематография скролла</b></summary>
<br>

- Lenis (плавный скролл) + GSAP ScrollTrigger.
- Мастер-таймлайн камеры с мягким easing (power2/power3) и паузами между секциями.
- Вступительный пролёт камеры сквозь облака (5.4 с после прелоадера).
- Сквозной HUD «эшелона»: FL 035 → FL 410 по мере прокрутки.
- Появление контента со stagger, счётчики цифр, плавные якоря.
</details>

<details>
<summary><b>Производительность и доступность</b></summary>
<br>

- Лёгкий режим 3D на мобильных и слабых устройствах: меньше облаков, без depth of field,
  сниженный pixel ratio + автоматическая деградация при падении FPS.
- Без WebGL — фолбэк на CSS-градиент, сайт остаётся читаемым.
- `prefers-reduced-motion` — статичный кадр, без Lenis и полёта камеры.
- Полная очистка ресурсов при уходе со страницы: dispose геометрий/материалов/текстур,
  kill ScrollTrigger, destroy Lenis.
- SRI-хэши на CDN-скриптах, ноль ошибок в консоли.
</details>

---

## ❓ Если что-то пошло не так

| Проблема | Решение |
|---|---|
| Чёрный или пустой экран | Вы открыли файл двойным кликом (`file://`). Запустите локальный сервер — см. [«Как запустить»](#-как-запустить) |
| Нет шрифтов / 3D не грузится | Проверьте интернет — библиотеки и шрифты идут с CDN |
| Тормозит на слабом устройстве | Это учтено: шаблон сам включит облегчённый режим. Если всё равно тяжело — уменьшите число облаков в `js/scene.js` |
| Анимации не двигаются | Возможно, в системе включено «уменьшение движения» (reduced motion) — шаблон честно его уважает |
